// wifi_secrets.h.example — template for local-only credentials.
// Copy to wifi_secrets.h (gitignored) and fill in your home WiFi + the
// URL of the machine serving pixel_post firmware for the Mac-OTA route.
#pragma once

#define PP_MAC_OTA_SSID  "your-wifi-ssid"
#define PP_MAC_OTA_PSK   "your-wifi-password"
#define PP_MAC_OTA_URL   "http://192.168.1.100/upd/pixel_post.ino.bin"
